using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ElevatorButtons : MonoBehaviour
{
    public bool isPushed = false;
    
    void Update()
    {
        if (isPushed == true)
        {
            //if button is room A, B, or C (run that script)
                //different scripts or new almighty script for variables
            //set something to return to false (when doors open to new room)
        }
    }
}
